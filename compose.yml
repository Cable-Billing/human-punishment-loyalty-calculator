services:
  webserver:
    container_name: web-server
    restart: unless-stopped
    build:
      context: .
      dockerfile: web-server/Dockerfile
    networks:
      - internal-network
    ports:
      - "8080:80"

networks:
  internal-network: